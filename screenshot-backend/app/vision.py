from typing import List
import os

def get_image_labels(image_path: str) -> list:
    """
    Get labels/tags for an image.
    For now, return empty list as we don't have ML model yet.
    """
    return []
